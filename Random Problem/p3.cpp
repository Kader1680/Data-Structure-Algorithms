
#include<iostream>
#include<vector>

std::string lengthOfLastWord(vector <string> s) {
    
    std::string ele = "ds";

    // for(int i=0; i < s.size(); i++){
        
        
    // }

    return ele;
}


int main(int argc, char const *argv[])
{
    std::string name = "hellow world";
    name.comp
    vector <string> li = {"hello", "world"};
    // std::cout << lengthOfLastWord(li) << std::endl;

    for (int i : li){
        std::cout << i<< std::endl;
    }
    
    return 0;
}
